# TODO

- left-align TOC
- change date for 3 articles on the homepage

/arhiv
- make histogram blue as H1, increase space between them
- stretch out the searchbar to the ~same size as the UI on the opposite site
- filter badges shift layout when appearing
- sortby can still be empty, make it fixed-width so it doesn't resize
- table-view sentinel row can be goofy, it doesn't load the next page until i scroll up a bit

## later

- submit sitemap to bing webmaster and indexnow. AIs use bing.
- regenerate titles (`/novica/amp-pa-dolenjske-jame-2` has different db and algolia names, slug is weird too, for example)